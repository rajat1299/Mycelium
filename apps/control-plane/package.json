{
  "name": "@computer-oss/control-plane",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "node --watch --env-file-if-exists=.env.local --import tsx src/server.ts",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@computer-oss/artifacts": "workspace:*",
    "@computer-oss/db": "workspace:*",
    "@computer-oss/orchestrator": "workspace:*",
    "@computer-oss/protocol": "workspace:*",
    "@computer-oss/router": "workspace:*",
    "@computer-oss/sandbox": "workspace:*",
    "fastify": "^5.6.1",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "tsx": "^4.20.5"
  }
}
